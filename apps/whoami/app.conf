# ⚠️ Fichier déclaratif uniquement — aucune logique, aucun affichage

APP_NAME="whoami"
APP_DESCRIPTION="Application de test Traefik (whoami)"

# Domaine par défaut (template)
APP_DEFAULT_DOMAIN="whoami.{{CF_DOMAIN}}"
