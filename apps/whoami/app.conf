# ⚠️ Toutes les valeurs doivent être entre guillemets (fichier sourcé par bash)
# Identification
info "DEBUG — CF_DOMAIN=$CF_DOMAIN"
info "DEBUG — APP_DEFAULT_DOMAIN=$APP_DEFAULT_DOMAIN"

APP_NAME="whoami"
APP_DESCRIPTION="Application de test Traefik (whoami)"

# Domaine par défaut
APP_DEFAULT_DOMAIN="whoami.${CF_DOMAIN}"
